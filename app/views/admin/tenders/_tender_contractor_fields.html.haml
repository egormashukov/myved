%fieldset
  = f.label :contractor_id
  = f.select :contractor_id, options_from_collection_for_select(current_contractor.friends , 'id', 'title')
  = f.hidden_field :_destroy
  = link_to 'удалить', '#', class: 'remove_fields'