= content_for :centered do
  %div{class: "#{'_topped' unless contractor_signed_in?}"}
    = render 'layouts/bar', header_t: @page.title, background_image: 'top_panel_card', left: true
    .page-body= raw @page.body